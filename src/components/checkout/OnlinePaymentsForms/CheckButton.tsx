import Button from "../../common/Button";

const CheckButton = () => {
    return (
        <div className="text-center mt-3">
            {/* Sumbit Button */}
            <Button type="submit" className="mt-2 bg-primary w-52 font-semibold">
                Next
            </Button>
        </div>
    );
};

export default CheckButton;
